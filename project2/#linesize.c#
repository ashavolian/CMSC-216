#include <studio.h>

int nextline(char line[]);

int main(void)
{
  


  
}

int nextline(char s[])
{
  int i = 0, c;

  while((c = getchar()) != EOF && c != '\n')
    {
      s[i] = c;
      i++;
    }
  return i++;
}

